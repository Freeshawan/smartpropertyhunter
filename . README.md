# SmartPropertyHunter (Next.js + PostgreSQL)

## 🚀 Lancer en local

1. Copier `.env.local.example` en `.env.local` et remplir la `DATABASE_URL`.
2. Installer les dépendances :
   ```bash
   npm install
